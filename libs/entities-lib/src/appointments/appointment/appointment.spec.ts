describe('>>> AppointmentEntity', () => {
  describe('>> constructor', () => {
    it('should instantiate name', () => {
      const name = 'name';
      expect(name).toEqual('name');
    });
  });
});
