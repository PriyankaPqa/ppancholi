{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "types": ["cypress", "cypress-xpath", "@cypress/grep"]
  },
  "include": [
    // add all files in which you see the "parserOptions.project" error
    "**/**.js",
    "**/**.ts"
  ],
  "exclude": [
    "node_modules"
  ]
}
